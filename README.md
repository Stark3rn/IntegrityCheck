# IntegrityCheck
A project searching to analyse local files on a computer

## Prerequisites
To use this project, here are the libraries needed to work on your machine
> pip install pyssdeep six python-magic-bin PyQt5